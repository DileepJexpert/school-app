import 'package:flutter/material.dart';

class CampusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Campus Page new", style: Theme.of(context).textTheme.headlineMedium),
    );
  }
}
